# Verification of the original findings

Each finding from the audit brief, independently reproduced and closed with a status from the brief's model.

| # | Original finding | Verified? | What was actually true | Status |
|---|---|---|---|---|
| 1 | Mobile `PROD_API_URL = 'http://192.168.1.53:8000/api'`; test `the production fallback is https` fails | Yes (at HEAD) | Also: `API_BASE_URL` was resolved without `isDev`, so a dev build with an `http://` LAN override would have been replaced. The other session's in-flight fix invented `https://api.niss.pro/api`, a host the repo never documents. | CONFIGURATION_FIXED — default is `https://niss.pro/api` (documented in the operator runbook §9, Android spec and codebase map); `__DEV__` wired; malformed/insecure overrides fall back with a warning; 5 new tests. **File later deleted from disk by an external action — see remaining-risks #2.** |
| 2 | `MODULE_TYPELESS_PACKAGE_JSON` warning | Yes | Mobile package is hybrid: Babel-compiled ESM app code plus CommonJS `babel.config.js` / `metro.config.js` loaded by Node. | CONFIRMED_NOT_A_BUG / ACCEPTED_RISK — adding `"type": "module"` would break Metro and Babel config loading. |
| 3 | `react-hooks/set-state-in-effect` in `CandidateQuiz.jsx` (~100, ~254) | Yes | The other session hid both with `setTimeout(…,0)` and `await Promise.resolve()`. A real defect sat underneath: when an automatic submit failed with a non-success response, the timer effect re-ran on every `submitting` toggle and resubmitted in a tight loop. | CONFIRMED_FIXED — `load` is a promise chain setting state only in callbacks; expiry handled inside the timer callback with a once-per-attempt guard reset on start; a failed auto-submit reloads server state. |
| 4 | `set-state-in-effect` in `MediclaimCardVerify.jsx` (~36) | Yes | The other session deleted the reset, so a token change would show the previous card until the response. | CONFIRMED_FIXED — result keyed by token; loading derived. |
| 5 | `set-state-in-effect` and `preserve-manual-memoization` in `Team.jsx` (~99) | Partially | No `Team.jsx` exists; the code is `src/pages/employee/ManagerSection.jsx`. The other session had replaced the dependency list with the whole `user` object and wrapped the call in `setTimeout`. | CONFIRMED_FIXED — primitive `accessToken`/`tokenType`, memoised filters, request-key derived loading, refresh via reload counter, error log no longer dumps the error object. |
| 6 | Useless assignments in `payslipUtils.js` (`perfo`, `other`, `computedGrossSalary`, `earningRows`, `deductionRows`) | Yes | Every branch assigns each variable before use, so the defaults were dead. | CONFIRMED_FIXED — change kept. Differential test: HEAD vs current `buildPayslipData` produced byte-identical output for 5,000 generated inputs across 5 company ids (missing/null/zero/string/negative/garbage values); a planted formula change was detected. Existing payslip tests 4/4. |
| 7 | Empty catch blocks in `pdfUtils.js` (~84, ~101) | Yes | Both guard expected, recoverable image-inlining failures (CORS fetch, canvas taint). The other session only added comments inside the empty blocks. | CONFIRMED_FIXED — explicit `fetch → canvas → keep original src` fallback chain with helper functions; nothing logged (document URLs may be sensitive). |
| 8 | 792 ESLint errors / 21 warnings | Yes (775/21 on reproduction) | 272 of the errors were runtime crashes, not style: 253 `no-undef` and 3 parse errors in Workforce and Recruitment pages, 16 `rules-of-hooks` in the Workforce list factory. | CONFIRMED_FIXED — **0 errors / 0 warnings**; no rule disabled, no config change; 23 existing `eslint-disable` comments removed. |
| 9 | Laravel 13 failures / 1,143 ("role/permission seed timing, fixture mismatches, table mocks") | No — 83 failing | The explanations in the brief were not supported. Causes were permission gates contradicting their own documentation, two company-scoping regressions exposing cross-tenant data, a password-reset rewrite that reopened enumeration and allowed account takeover, a self-approval gap, a mis-named table, a model mirror that never propagated updates, a seeder that missed two pages, Postgres transaction poisoning, and stale tests/fixtures. | See `backend-test-failures.md` for all 83. **Final: see `quality-gates-final.json`.** |
| 10 | Node service `tsc` 0 errors "is not complete QA" | Yes | Vitest suite had 6 failures: 5 from a JWT fixture that expired on 2026-09-02, 1 real copy defect (`Login.jsx` hard-coded "6-digit OTP"). | TEST_DEFECT_FIXED + CONFIRMED_FIXED — tests pinned to the fixture's issue time plus a new expiry-boundary test; copy uses `OTP_LENGTH`. 650 pass / 7 skipped (integration, no DB URL). |
