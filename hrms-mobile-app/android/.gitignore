# Signing secrets — never commit these.
# Losing them means never being able to update the Play Store listing again,
# so keep an offline backup somewhere separate from the repo.
keystore.properties
app/niss-release.keystore
*.jks

# Build output
build/
app/build/
.gradle/
local.properties
