import { z } from 'zod';

export const trainingProgramQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const trainingProgramParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createTrainingProgramSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateTrainingProgramSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const trainingCourseQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const trainingCourseParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createTrainingCourseSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateTrainingCourseSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const trainingSessionQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const trainingSessionParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createTrainingSessionSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateTrainingSessionSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const enrollmentQuerySchema = z.object({ query: z.object({ status: z.string().optional() }) });
export const enrollmentParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createEnrollmentSchema = z.object({ body: z.object({ courseId: z.string().optional() }) });
export const updateEnrollmentSchema = z.object({ body: z.object({ status: z.string().optional() }) });
export const attendanceQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const attendanceParamsSchema = z.object({ params: z.object({ attendanceId: z.string().min(1) }) });
export const markAttendanceSchema = z.object({ body: z.object({ present: z.boolean().optional() }) });
export const assessmentQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const assessmentParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createAssessmentSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateAssessmentSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const assessmentAttemptQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const assessmentAttemptParamsSchema = z.object({ params: z.object({ attemptId: z.string().min(1) }) });
export const submitAssessmentSchema = z.object({ body: z.object({ answers: z.any().optional() }) });
export const gradeAssessmentSchema = z.object({ body: z.object({ score: z.number().optional() }) });
export const feedbackQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const feedbackParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const submitFeedbackSchema = z.object({ body: z.object({ rating: z.number().optional() }) });
export const certificateQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const certificateParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createCertificateSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const trainingBudgetQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const trainingBudgetParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createTrainingBudgetSchema = z.object({ body: z.object({ amount: z.number().optional() }) });
export const updateTrainingBudgetSchema = z.object({ body: z.object({ amount: z.number().optional() }) });
export const trainingReportQuerySchema = z.object({ query: z.object({ year: z.coerce.number().optional() }) });
export const skillQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const skillParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createSkillSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateSkillSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const learningPathQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const learningPathParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createLearningPathSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateLearningPathSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const cancelSessionSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const rejectEnrollmentSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const withdrawEnrollmentSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const issueCertificateSchema = z.object({ body: z.object({ code: z.string().optional() }) });
export const revokeCertificateSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const addBudgetExpenseSchema = z.object({ body: z.object({ amount: z.number().optional() }) });
export const updateBudgetExpenseSchema = z.object({ body: z.object({ amount: z.number().optional() }) });
export const bulkEnrollSchema = z.object({ body: z.object({ employeeIds: z.array(z.string()).optional() }) });
export const bulkAttendanceSchema = z.object({ body: z.object({ records: z.array(z.any()).optional() }) });
export const bulkGradeSchema = z.object({ body: z.object({ grades: z.array(z.any()).optional() }) });
export const bulkIssueCertificatesSchema = z.object({ body: z.object({ certificates: z.array(z.any()).optional() }) });
export const bulkCreateSessionsSchema = z.object({ body: z.object({ sessions: z.array(z.any()).optional() }) });
