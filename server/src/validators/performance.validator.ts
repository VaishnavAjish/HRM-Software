import { z } from 'zod';

export const reviewCycleQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const reviewCycleParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createReviewCycleSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateReviewCycleSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const performanceReviewQuerySchema = z.object({ query: z.object({ status: z.string().optional() }) });
export const performanceReviewParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createPerformanceReviewSchema = z.object({ body: z.object({ employeeId: z.string().optional() }) });
export const updatePerformanceReviewSchema = z.object({ body: z.object({ rating: z.number().optional() }) });
export const submitSelfAssessmentSchema = z.object({ body: z.object({ comments: z.string().optional() }) });
export const submitManagerAssessmentSchema = z.object({ body: z.object({ rating: z.number().optional() }) });
export const finalizeReviewSchema = z.object({ body: z.object({ finalRating: z.number().optional() }) });
export const goalQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const goalParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createGoalSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateGoalSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const goalProgressSchema = z.object({ body: z.object({ progress: z.number().optional() }) });
export const competencyQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const competencyParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createCompetencySchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateCompetencySchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const competencyAssessmentSchema = z.object({ body: z.object({ score: z.number().optional() }) });
export const developmentPlanQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const developmentPlanParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createDevelopmentPlanSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateDevelopmentPlanSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateDevelopmentPlanProgressSchema = z.object({ body: z.object({ progress: z.number().optional() }) });
export const performanceReportQuerySchema = z.object({ query: z.object({ year: z.coerce.number().optional() }) });
export const calibrationQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const calibrationParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createCalibrationSessionSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateCalibrationSessionSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const submitCalibrationRatingSchema = z.object({ body: z.object({ rating: z.number().optional() }) });
export const okrQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const okrParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createOKRSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateOKRSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateOKRProgressSchema = z.object({ body: z.object({ progress: z.number().optional() }) });
export const feedbackQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const feedbackParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createFeedbackSchema = z.object({ body: z.object({ text: z.string().optional() }) });
export const updateFeedbackSchema = z.object({ body: z.object({ text: z.string().optional() }) });
export const acknowledgeFeedbackSchema = z.object({ body: z.object({ comments: z.string().optional() }) });
export const assignReviewersSchema = z.object({ body: z.object({ reviewerIds: z.array(z.string()).optional() }) });
export const bulkCreateReviewsSchema = z.object({ body: z.object({ reviews: z.array(z.any()).optional() }) });
export const addReviewCommentSchema = z.object({ body: z.object({ comment: z.string().optional() }) });
export const alignGoalSchema = z.object({ body: z.object({ parentGoalId: z.string().optional() }) });
export const createCompetencyFrameworkSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const addDevelopmentActivitySchema = z.object({ body: z.object({ activity: z.string().optional() }) });
export const completeDevelopmentActivitySchema = z.object({ body: z.object({ activityId: z.string().optional() }) });
export const alignOKRSchema = z.object({ body: z.object({ parentOkrId: z.string().optional() }) });
export const createKeyResultSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const keyResultParamsSchema = z.object({ params: z.object({ keyResultId: z.string().min(1) }) });
export const updateKeyResultSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const requestFeedbackSchema = z.object({ body: z.object({ recipientId: z.string().optional() }) });
export const createFeedbackTemplateSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const bulkCreateGoalsSchema = z.object({ body: z.object({ goals: z.array(z.any()).optional() }) });
export const bulkSendFeedbackSchema = z.object({ body: z.object({ feedback: z.array(z.any()).optional() }) });
