import { z } from 'zod';

export const candidateQuerySchema = z.object({ query: z.object({ search: z.string().optional(), page: z.coerce.number().optional(), limit: z.coerce.number().optional() }) });
export const candidateParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createCandidateSchema = z.object({ body: z.object({ firstName: z.string().min(1), lastName: z.string().min(1), email: z.string().email() }) });
export const updateCandidateSchema = z.object({ body: z.object({ firstName: z.string().optional(), lastName: z.string().optional() }) });
export const jobPostingQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const jobPostingParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createJobPostingSchema = z.object({ body: z.object({ title: z.string().min(1), departmentId: z.string().min(1) }) });
export const updateJobPostingSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const applicationQuerySchema = z.object({ query: z.object({ status: z.string().optional() }) });
export const applicationParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createApplicationSchema = z.object({ body: z.object({ jobId: z.string().min(1), candidateId: z.string().min(1) }) });
export const updateApplicationStatusSchema = z.object({ body: z.object({ status: z.string().min(1) }) });
export const scheduleInterviewSchema = z.object({ body: z.object({ scheduledTime: z.string().optional() }) });
export const submitInterviewFeedbackSchema = z.object({ body: z.object({ rating: z.number().optional(), comments: z.string().optional() }) });
export const createOfferSchema = z.object({ body: z.object({ ctc: z.number().optional() }) });
export const updateOfferSchema = z.object({ body: z.object({ ctc: z.number().optional() }) });
export const recruitmentStatsQuerySchema = z.object({ query: z.object({ year: z.coerce.number().optional() }) });
export const createNoteSchema = z.object({ body: z.object({ text: z.string().optional() }) });
export const addTagsSchema = z.object({ body: z.object({ tags: z.array(z.string()).optional() }) });
export const createCommunicationSchema = z.object({ body: z.object({ message: z.string().optional() }) });
export const uploadDocumentSchema = z.object({ body: z.object({ fileUrl: z.string().optional() }) });
export const moveStageSchema = z.object({ body: z.object({ stage: z.string().optional() }) });
export const declineOfferSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const convertToEmployeeSchema = z.object({ body: z.object({ joiningDate: z.string().optional() }) });
export const screenApplicationSchema = z.object({ body: z.object({ score: z.number().optional() }) });
export const rejectApplicationSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const interviewQuerySchema = z.object({ query: z.object({ page: z.coerce.number().optional() }) });
export const interviewParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const updateInterviewSchema = z.object({ body: z.object({ time: z.string().optional() }) });
export const rescheduleInterviewSchema = z.object({ body: z.object({ newTime: z.string().optional() }) });
export const cancelInterviewSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const offerQuerySchema = z.object({ query: z.object({ page: z.coerce.number().optional() }) });
export const offerParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const withdrawOfferSchema = z.object({ body: z.object({ reason: z.string().optional() }) });
export const bulkImportCandidatesSchema = z.object({ body: z.object({ candidates: z.array(z.any()).optional() }) });
export const bulkUpdateStatusSchema = z.object({ body: z.object({ status: z.string().optional() }) });
export const bulkImportApplicationsSchema = z.object({ body: z.object({ applications: z.array(z.any()).optional() }) });
export const bulkScheduleInterviewsSchema = z.object({ body: z.object({ interviews: z.array(z.any()).optional() }) });
