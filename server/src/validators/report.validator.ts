import { z } from 'zod';

export const reportQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const reportParamsSchema = z.object({ params: z.object({ templateId: z.string().optional(), reportId: z.string().optional(), id: z.string().optional() }) });
export const reportTemplateQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const reportTemplateParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createReportTemplateSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateReportTemplateSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const scheduledReportQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const scheduledReportParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createScheduledReportSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateScheduledReportSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const dashboardQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const dashboardParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createDashboardSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateDashboardSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const widgetQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const widgetParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createWidgetSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateWidgetSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const exportQuerySchema = z.object({ query: z.object({ format: z.string().optional() }) });
export const analyticsQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const workforceQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const headcountQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const turnoverQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const attendanceReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const leaveReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const payrollReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const recruitmentReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const performanceReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const trainingReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const complianceReportQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const shareDashboardSchema = z.object({ body: z.object({ userIds: z.array(z.string()).optional() }) });
export const reorderWidgetsSchema = z.object({ body: z.object({ widgetIds: z.array(z.string()).optional() }) });
export const generateAdhocReportSchema = z.object({ body: z.object({ type: z.string().optional() }) });
export const createCustomReportSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateCustomReportSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const kpiQuerySchema = z.object({ query: z.object({ category: z.string().optional() }) });
export const kpiParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const kpiTrendQuerySchema = z.object({ query: z.object({ period: z.string().optional() }) });
export const createKPISchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateKPISchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const scheduleExportSchema = z.object({ body: z.object({ format: z.string().optional() }) });
export const exportHistoryQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const exportParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
