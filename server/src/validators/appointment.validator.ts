import { z } from 'zod';

export const appointmentQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const appointmentParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createAppointmentSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateAppointmentSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const appointmentTypeQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const appointmentTypeParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createAppointmentTypeSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const updateAppointmentTypeSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const appointmentSlotQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const appointmentSlotParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createAppointmentSlotSchema = z.object({ body: z.object({ startTime: z.string().optional() }) });
export const updateAppointmentSlotSchema = z.object({ body: z.object({ startTime: z.string().optional() }) });
export const appointmentBookingQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const appointmentBookingParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createAppointmentBookingSchema = z.object({ body: z.object({ slotId: z.string().optional() }) });
export const updateAppointmentBookingSchema = z.object({ body: z.object({ status: z.string().optional() }) });
export const appointmentReminderSchema = z.object({ body: z.object({ time: z.string().optional() }) });
export const appointmentFeedbackSchema = z.object({ body: z.object({ rating: z.number().optional() }) });
export const appointmentReportQuerySchema = z.object({ query: z.object({ year: z.coerce.number().optional() }) });
export const shiftQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const shiftParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createShiftSchema = z.object({ body: z.object({ code: z.string().optional(), name: z.string().optional() }) });
export const updateShiftSchema = z.object({ body: z.object({ name: z.string().optional() }) });
export const shiftAssignmentQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const shiftAssignmentParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createShiftAssignmentSchema = z.object({ body: z.object({ shiftId: z.string().optional() }) });
export const updateShiftAssignmentSchema = z.object({ body: z.object({ shiftId: z.string().optional() }) });
export const rosterQuerySchema = z.object({ query: z.object({ search: z.string().optional() }) });
export const rosterParamsSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const createRosterSchema = z.object({ body: z.object({ title: z.string().optional() }) });
export const updateRosterSchema = z.object({ body: z.object({ title: z.string().optional() }) });
